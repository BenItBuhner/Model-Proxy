{
  "name": "model-proxy",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Model-Proxy: a fast, modular LLM routing proxy on Bun.",
  "scripts": {
    "dev": "bun run --hot ./src/cli/main.ts",
    "start": "bun run ./src/cli/main.ts",
    "build": "bun build ./src/cli/main.ts --target=bun --outfile=./dist/server.js",
    "build:web": "cd web && bun install && bun run build",
    "eval:fusion-quality": "bun test tests/fusion-complexity-scorer.test.ts tests/fusion-quality-gauntlet.test.ts tests/fusion-quality-scorecard.test.ts tests/fusion-scorecard-benchmark.test.ts",
    "eval:fusion-scorecard": "bun run benchmarks/fusion-quality-scorecard.ts",
    "test": "bun test",
    "test:responses-sdk": "bun run scripts/responses-openai-sdk-conformance.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.3",
    "@grpc/proto-loader": "^0.8.0",
    "hono": "^4.6.13",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "openai": "^6.49.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
