{
  "name": "@model-proxy/monorepo",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Model-Proxy: a fast, modular LLM routing proxy on Bun.",
  "workspaces": [
    "packages/*",
    "apps/cli",
    "apps/web"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/server dev",
    "start": "bun run --cwd packages/server start",
    "build": "bun run --cwd packages/server build",
    "build:web": "bun run --cwd apps/web build",
    "build:cli": "bun build --compile apps/cli/src/main.ts --outfile dist/model-proxy",
    "test": "bun run --cwd packages/server test && bun run --cwd apps/web test",
    "test:responses-sdk": "bun run --cwd packages/server test:responses-sdk",
    "eval:fusion-quality": "bun run --cwd packages/server eval:fusion-quality",
    "eval:fusion-scorecard": "bun run --cwd packages/server eval:fusion-scorecard",
    "typecheck": "bun run --cwd packages/contracts typecheck && bun run --cwd packages/server typecheck && bun run --cwd apps/web typecheck"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
